# PostgreSQL local Docker (pgvector) — port 5434, see docker-compose.yml
DATABASE_URL=postgresql://rag_user:rag_pass@localhost:5434/rag_db

# Apache AGE local Docker — port 5433, see docker-compose.yml
AGE_DATABASE_URL=postgresql://age_user:age_pass@localhost:5433/legal_graph
AGE_GRAPH_NAME=legal_graph
POSTGRES_TABLE_DOCUMENTS=documents
POSTGRES_TABLE_CHUNKS=chunks

# LLM Provider Configuration
# Options: openai, openrouter, ollama, gemini
# Available local models: llama3.1:8b, phi4:latest, llama3.2:3b
LLM_PROVIDER=ollama
LLM_API_KEY=ollama
LLM_MODEL=llama3.1:8b
LLM_BASE_URL=http://localhost:11434/v1

# Embedding Provider Configuration
# Available local models: nomic-embed-text:latest (768 dimensions)
EMBEDDING_PROVIDER=ollama
EMBEDDING_API_KEY=ollama
EMBEDDING_MODEL=nomic-embed-text:latest
EMBEDDING_BASE_URL=http://localhost:11434/v1
EMBEDDING_DIMENSION=768

# Search Configuration
DEFAULT_MATCH_COUNT=10
MAX_MATCH_COUNT=50
DEFAULT_TEXT_WEIGHT=0.3


# KG entity extraction — dedicated model for one-time entity extraction
KG_LLM_MODEL=qwen2.5:14b
KG_LLM_API_KEY=ollama
KG_LLM_BASE_URL=http://localhost:11434/v1
