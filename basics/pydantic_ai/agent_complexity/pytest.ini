[pytest]
# Local config so this example suite runs standalone regardless of repo-root
# settings. Run from this directory:  pytest -v
testpaths = tests
markers =
    ollama: live test that requires a running Ollama daemon (skipped by default)
addopts = -ra
